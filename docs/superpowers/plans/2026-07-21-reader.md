# 《时序残本》在线阅读器 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有 webnovel 仓库内搭建 React 阅读器，部署到 GitHub Pages，加新章只需 push 正文+索引、不重新构建站点。

**Architecture:** 站点本体（gh-pages 分支的 React 应用）与内容数据（main 分支的 raw markdown + JSON 索引）解耦。React 应用运行时通过 `raw.githubusercontent.com` 拉取章节索引与正文 markdown，marked 转 HTML 渲染。仓库已设为 Public，raw 支持 CORS `*`。

**Tech Stack:** Vite 5 + React 18 + marked；部署用 GitHub Actions → gh-pages 分支；测试用 Vitest + jsdom。

**关联设计文档：** `docs/superpowers/specs/2026-07-21-reader-design.md`

**仓库关键事实（实现时需对照）：**
- 仓库：`xSmile-art/shixu-canben`，已 Public，默认分支 `main`
- 正文路径：`novels/时序残本/正文/第NNN章-标题.md`（中文路径，运行时需 `encodeURI`）
- GitHub Pages 目标地址：`https://xsmile-art.github.io/shixu-canben/`（子路径，Vite `base` 必须设 `/shixu-canben/`）
- raw CDN 缓存 5 分钟（`Cache-Control: max-age=300`），新章 push 后最多 5 分钟生效

---

## 文件结构总览

```
webnovel/
├── chapters-index.json              ← 新建：章节索引（main 分支，手动维护）
├── reader/                           ← 新建：React 项目
│   ├── package.json
│   ├── vite.config.js
│   ├── index.html
│   ├── .gitignore                    ← node_modules/dist
│   ├── src/
│   │   ├── main.jsx                  ← 入口
│   │   ├── App.jsx                   ← 应用壳
│   │   ├── App.css                   ← 全局样式 + 明暗主题
│   │   ├── lib/
│   │   │   ├── raw.js                ← 拼 raw URL
│   │   │   ├── markdown.js            ← marked 配置
│   │   │   └── theme.js              ← 主题持久化
│   │   ├── hooks/
│   │   │   ├── useChapters.js
│   │   │   ├── useChapter.js
│   │   │   └── useReadingProgress.js
│   │   └── components/
│   │       ├── ChapterList.jsx
│   │       ├── ChapterView.jsx
│   │       ├── NavButtons.jsx
│   │       ├── Toolbar.jsx
│   │       └── LoadingError.jsx
│   └── test/
│       ├── lib/raw.test.js
│       ├── lib/markdown.test.js
│       ├── lib/theme.test.js
│       ├── hooks/useChapters.test.js
│       ├── hooks/useChapter.test.js
│       └── hooks/useReadingProgress.test.js
└── .github/workflows/
    ├── deploy.yml                    ← 构建并部署到 gh-pages
    └── ci.yml                       ← PR/push 跑测试
```

**职责边界：**
- `lib/raw.js`：唯一知道 raw URL 拼装规则与仓库坐标的地方。文件名 → URL 的映射逻辑集中于此，改仓库路径只动这一个文件。
- `lib/markdown.js`：唯一持有 marked 实例的地方，解析配置集中。
- `lib/theme.js`：主题枚举 + localStorage 读写，组件不直接碰 localStorage。
- `hooks/useChapter.js`：唯一知道"章号 → fetch → marked → HTML"链路的地方，带模块级缓存。
- `hooks/useReadingProgress.js`：唯一读写阅读位置 localStorage 的地方。
- 组件层只组合 hooks 与 lib，不包含数据获取逻辑。

---

## Task 1: 初始化 Vite + React 项目骨架

**Files:**
- Create: `reader/package.json`
- Create: `reader/vite.config.js`
- Create: `reader/index.html`
- Create: `reader/.gitignore`
- Create: `reader/src/main.jsx`
- Create: `reader/src/App.jsx`
- Create: `reader/src/App.css`

- [ ] **Step 1: 创建 reader/package.json**

```json
{
  "name": "shixu-canben-reader",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "marked": "^12.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "jsdom": "^24.1.0",
    "vitest": "^1.6.0"
  }
}
```

- [ ] **Step 2: 创建 reader/vite.config.js**

注意 `base: '/shixu-canben/'`——GitHub Pages 子路径部署必需。`test` 字段把 Vitest 的 jsdom 环境与同项目路径共享配置。

```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  base: '/shixu-canben/',
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    include: ['test/**/*.test.js'],
    coverage: { reporter: [] }
  }
})
```

- [ ] **Step 3: 创建 reader/index.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>时序残本 · 在线阅读</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

- [ ] **Step 4: 创建 reader/.gitignore**

```
node_modules/
dist/
*.local
.vitest
```

- [ ] **Step 5: 创建 reader/src/main.jsx**

```jsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './App.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
```

- [ ] **Step 6: 创建 reader/src/App.jsx 与 App.css 占位**

`reader/src/App.jsx`：

```jsx
export default function App() {
  return (
    <div className="app">
      <h1>时序残本</h1>
      <p>阅读器骨架，待实现。</p>
    </div>
  )
}
```

`reader/src/App.css`（最小样式，后续 Task 9 扩展）：

```css
* { box-sizing: border-box; }
body { margin: 0; font-family: system-ui, -apple-system, "PingFang SC", "Microsoft YaHei", sans-serif; }
.app { max-width: 720px; margin: 2rem auto; padding: 0 1rem; }
```

- [ ] **Step 7: 安装依赖并验证 dev 能启动**

Run: `cd reader && npm install && npm run build`
Expected: 构建成功，生成 `reader/dist/index.html`，无报错。

- [ ] **Step 8: 提交骨架**

```bash
cd /d/workspace/ai/webnovel
git add reader/ .gitignore
git commit -m "feat(reader): 初始化 Vite + React 项目骨架"
```

---

## Task 2: raw URL 拼装工具（TDD）

**Files:**
- Create: `reader/src/lib/raw.js`
- Create: `reader/test/lib/raw.test.js`

`raw.js` 是唯一知道仓库坐标与路径拼装规则的地方。把仓库元数据（owner/repo/branch/novels 前缀）集中为常量，后续仓库迁移只动这里。

- [ ] **Step 1: 写失败测试 `reader/test/lib/raw.test.js`**

```js
import { describe, it, expect } from 'vitest'
import { buildIndexUrl, buildChapterUrl, buildFileName } from '../../src/lib/raw.js'

describe('buildFileName', () => {
  it('把章号与标题拼成 文件名', () => {
    expect(buildFileName({ num: '001', title: '草稿层' })).toBe('第001章-草稿层.md')
  })
  it('标题含数字也原样拼接', () => {
    expect(buildFileName({ num: '010', title: '定调' })).toBe('第010章-定调.md')
  })
})

describe('buildChapterUrl', () => {
  it('生成中文路径被 encodeURI 编码的 raw URL', () => {
    const url = buildChapterUrl({ num: '001', title: '草稿层' })
    expect(url).toBe(
      'https://raw.githubusercontent.com/xSmile-art/shixu-canben/main/novels/%E6%97%B6%E5%BA%8F%E6%AE%8B%E6%9C%AC/%E6%AD%A3%E6%96%87/%E7%AC%AC001%E7%AB%A0-%E8%8D%89%E7%A8%BF%E5%B1%82.md'
    )
  })
  it('章号 010 也能正确编码', () => {
    const url = buildChapterUrl({ num: '010', title: '定调' })
    expect(url).toBe(
      'https://raw.githubusercontent.com/xSmile-art/shixu-canben/main/novels/%E6%97%B6%E5%BA%8F%E6%AE%8B%E6%9C%AC/%E6%AD%A3%E6%96%87/%E7%AC%AC010%E7%AB%A0-%E5%AE%9A%E8%B0%83.md'
    )
  })
})

describe('buildIndexUrl', () => {
  it('返回索引 raw URL', () => {
    expect(buildIndexUrl()).toBe(
      'https://raw.githubusercontent.com/xSmile-art/shixu-canben/main/chapters-index.json'
    )
  })
})
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd reader && npx vitest run test/lib/raw.test.js`
Expected: FAIL，提示 `Cannot find module '../../src/lib/raw.js'`。

- [ ] **Step 3: 实现 `reader/src/lib/raw.js`**

注意：`encodeURI` 会对 `:` 之外的所有非 ASCII 编码，但保留 `/`。由于"时序残本/正文/"是路径分隔，必须保留 `/`，`encodeURI` 正好满足（它不编码 `/`）。章号段是 ASCII 不受影响。

```js
// 仓库坐标——迁移仓库只改这里
const OWNER = 'xSmile-art'
const REPO = 'shixu-canben'
const BRANCH = 'main'
const NOVELS_PREFIX = 'novels/时序残本/正文'

const RAW_BASE = `https://raw.githubusercontent.com/${OWNER}/${REPO}/${BRANCH}`

// 把 { num, title } 还原成仓库里的文件名（与创作命名规则一致）
export function buildFileName({ num, title }) {
  return `第${num}章-${title}.md`
}

// 章节正文 raw URL，中文路径用 encodeURI 编码
export function buildChapterUrl(chapter) {
  const fileName = buildFileName(chapter)
  const path = `${NOVELS_PREFIX}/${fileName}`
  return `${RAW_BASE}/${encodeURI(path)}`
}

// 章节索引 raw URL
export function buildIndexUrl() {
  return `${RAW_BASE}/chapters-index.json`
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd reader && npx vitest run test/lib/raw.test.js`
Expected: PASS，3 个 describe 全绿。

- [ ] **Step 5: 提交**

```bash
cd /d/workspace/ai/webnovel
git add reader/src/lib/raw.js reader/test/lib/raw.test.js
git commit -m "feat(reader): raw URL 拼装工具 + 测试"
```

---

## Task 3: markdown 解析工具（TDD）

**Files:**
- Create: `reader/src/lib/markdown.js`
- Create: `reader/test/lib/markdown.test.js`

- [ ] **Step 1: 写失败测试 `reader/test/lib/markdown.test.js`**

```js
import { describe, it, expect } from 'vitest'
import { renderMarkdown } from '../../src/lib/markdown.js'

describe('renderMarkdown', () => {
  it('把一级标题 markdown 转成 h1 HTML', () => {
    expect(renderMarkdown('# 第一章 草稿层')).toBe('<h1>第一章 草稿层</h1>\n')
  })
  it('把段落转成 p', () => {
    expect(renderMarkdown('后半夜的跨海桥没什么人。')).toBe('<p>后半夜的跨海桥没什么人。</p>\n')
  })
  it('空行分隔产生多个段落', () => {
    const src = '第一段。\n\n第二段。'
    expect(renderMarkdown(src)).toBe('<p>第一段。</p>\n<p>第二段。</p>\n')
  })
  it('对内联 HTML 做转义（安全）', () => {
    expect(renderMarkdown('<script>alert(1)</script>')).not.toContain('<script>')
  })
})
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd reader && npx vitest run test/lib/markdown.test.js`
Expected: FAIL，`Cannot find module '../../src/lib/markdown.js'`。

- [ ] **Step 3: 实现 `reader/src/lib/markdown.js`**

marked 12 默认对内联 HTML 会被 `mangle`/`headerIds` 等选项影响，显式关闭非必要选项并开启转义。`marked.parse` 同步可用（不传 async 选项）。

```js
import { marked } from 'marked'

marked.setOptions({
  gfm: true,
  breaks: false,
  headerIds: false,
  mangle: false
})

// markdown 字符串 → HTML 字符串。marked 默认转义内联 HTML，正文为自有内容无注入风险。
export function renderMarkdown(md) {
  return marked.parse(md)
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd reader && npx vitest run test/lib/markdown.test.js`
Expected: PASS，4 个用例全绿。

> 如 `headerIds`/`mangle` 选项在当前 marked 版本报错（marked 12 已移除这些选项），改成只保留 `gfm` 与 `breaks` 即可，删掉报错的行并重跑测试。

- [ ] **Step 5: 提交**

```bash
cd /d/workspace/ai/webnovel
git add reader/src/lib/markdown.js reader/test/lib/markdown.test.js
git commit -m "feat(reader): markdown 解析工具 + 测试"
```

---

## Task 4: 主题持久化工具（TDD）

**Files:**
- Create: `reader/src/lib/theme.js`
- Create: `reader/test/lib/theme.test.js`

- [ ] **Step 1: 写失败测试 `reader/test/lib/theme.test.js`**

```js
import { describe, it, expect, beforeEach } from 'vitest'
import { getTheme, setTheme, THEME_KEY } from '../../src/lib/theme.js'

describe('theme', () => {
  beforeEach(() => {
    localStorage.clear()
  })

  it('无记录时默认 light', () => {
    expect(getTheme()).toBe('light')
  })
  it('setTheme 后 getTheme 能读回', () => {
    setTheme('dark')
    expect(getTheme()).toBe('dark')
  })
  it('setTheme 写入 localStorage 的 key 为 THEME_KEY', () => {
    setTheme('dark')
    expect(localStorage.getItem(THEME_KEY)).toBe('dark')
  })
})
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd reader && npx vitest run test/lib/theme.test.js`
Expected: FAIL，`Cannot find module '../../src/lib/theme.js'`。

- [ ] **Step 3: 实现 `reader/src/lib/theme.js`**

```js
export const THEME_KEY = 'sxcb-theme'

// 读主题，无记录默认 light
export function getTheme() {
  const v = localStorage.getItem(THEME_KEY)
  return v === 'dark' ? 'dark' : 'light'
}

// 写主题并持久化
export function setTheme(theme) {
  localStorage.setItem(THEME_KEY, theme)
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd reader && npx vitest run test/lib/theme.test.js`
Expected: PASS，3 个用例全绿。

- [ ] **Step 5: 提交**

```bash
cd /d/workspace/ai/webnovel
git add reader/src/lib/theme.js reader/test/lib/theme.test.js
git commit -m "feat(reader): 主题持久化工具 + 测试"
```

---

## Task 5: useChapters hook（TDD）

**Files:**
- Create: `reader/src/hooks/useChapters.js`
- Create: `reader/test/hooks/useChapters.test.js`

`useChapters` 只负责拉索引并暴露 `{ chapters, status, error, retry }`。fetch 用 `globalThis.fetch`，测试时注入 mock。

- [ ] **Step 1: 写失败测试 `reader/test/hooks/useChapters.test.js`**

测试用 `renderHook` + `act`。首次 render 触发 fetch，`act` 推进异步。状态机：`idle → loading → success|error`。

```js
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { renderHook, act, waitFor } from '@testing-library/react'
import { useChapters } from '../../src/hooks/useChapters.js'
import { buildIndexUrl } from '../../src/lib/raw.js'

const SAMPLE_INDEX = [
  { num: '001', title: '草稿层' },
  { num: '002', title: '十秒' }
]

describe('useChapters', () => {
  beforeEach(() => {
    vi.restoreAllMocks()
  })

  it('初始 status 为 loading，fetch 成功后 status=success 且 chapters 填充', async () => {
    const fetchMock = vi.fn(() => Promise.resolve({
      ok: true,
      json: () => Promise.resolve(SAMPLE_INDEX)
    }))
    vi.stubGlobal('fetch', fetchMock)

    const { result } = renderHook(() => useChapters())
    expect(result.current.status).toBe('loading')

    await waitFor(() => expect(result.current.status).toBe('success'))
    expect(result.current.chapters).toEqual(SAMPLE_INDEX)
    expect(result.current.error).toBeNull()
    expect(fetchMock).toHaveBeenCalledWith(buildIndexUrl())
  })

  it('fetch 失败时 status=error 且带 error 信息，retry 可重试', async () => {
    const fetchMock = vi.fn(() => Promise.resolve({ ok: false, status: 404 }))
    vi.stubGlobal('fetch', fetchMock)

    const { result } = renderHook(() => useChapters())
    await waitFor(() => expect(result.current.status).toBe('error'))
    expect(result.current.error).toBeTruthy()
    expect(result.current.chapters).toEqual([])

    // 第二次让它成功
    fetchMock.mockImplementation(() => Promise.resolve({
      ok: true, json: () => Promise.resolve(SAMPLE_INDEX)
    }))
    act(() => { result.current.retry() })
    await waitFor(() => expect(result.current.status).toBe('success'))
    expect(result.current.chapters).toEqual(SAMPLE_INDEX)
  })
})
```

> 注意：`renderHook`/`waitFor` 来自 `@testing-library/react`，需在 Task 5 的 Step 0 加进 devDependencies（见下）。

- [ ] **Step 0: 补装 testing-library 依赖**

Run: `cd reader && npm install -D @testing-library/react@^16.0.0 @testing-library/jest-dom@^6.4.0`
（安装后无需在测试里 import jest-dom，断言用原生 expect。）

- [ ] **Step 2: 运行测试验证失败**

Run: `cd reader && npx vitest run test/hooks/useChapters.test.js`
Expected: FAIL，`Cannot find module '../../src/hooks/useChapters.js'`。

- [ ] **Step 3: 实现 `reader/src/hooks/useChapters.js`**

```js
import { useEffect, useState, useCallback } from 'react'
import { buildIndexUrl } from '../lib/raw.js'

// 拉取章节索引。status: 'loading' | 'success' | 'error'
export function useChapters() {
  const [chapters, setChapters] = useState([])
  const [status, setStatus] = useState('loading')
  const [error, setError] = useState(null)

  const load = useCallback(async () => {
    setStatus('loading')
    setError(null)
    try {
      const res = await fetch(buildIndexUrl())
      if (!res.ok) throw new Error(`索引加载失败 (HTTP ${res.status})`)
      const data = await res.json()
      setChapters(Array.isArray(data) ? data : [])
      setStatus('success')
    } catch (e) {
      setError(e.message || '未知错误')
      setStatus('error')
    }
  }, [])

  useEffect(() => { load() }, [load])

  const retry = useCallback(() => { load() }, [load])
  return { chapters, status, error, retry }
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd reader && npx vitest run test/hooks/useChapters.test.js`
Expected: PASS，2 个用例全绿。

- [ ] **Step 5: 提交**

```bash
cd /d/workspace/ai/webnovel
git add reader/src/hooks/useChapters.js reader/test/hooks/useChapters.test.js reader/package.json reader/package-lock.json
git commit -m "feat(reader): useChapters hook + 测试"
```

---

## Task 6: useChapter hook（TDD）

**Files:**
- Create: `reader/src/hooks/useChapter.js`
- Create: `reader/test/hooks/useChapter.test.js`

单章加载，带模块级缓存（同一章号多次调用只 fetch 一次）。fetch raw markdown → `renderMarkdown` → HTML。

- [ ] **Step 1: 写失败测试 `reader/test/hooks/useChapter.test.js`**

```js
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { renderHook, waitFor } from '@testing-library/react'
import { useChapter } from '../../src/hooks/useChapter.js'
import { __resetChapterCache } from '../../src/hooks/useChapter.js'

const CHAPTER = { num: '001', title: '草稿层' }
const MARKDOWN = '# 第一章 草稿层\n\n后半夜的跨海桥。'

describe('useChapter', () => {
  beforeEach(() => {
    vi.restoreAllMocks()
    __resetChapterCache()
  })

  it('初始 loading，成功后返回 HTML 并标记 success', async () => {
    const fetchMock = vi.fn(() => Promise.resolve({
      ok: true, text: () => Promise.resolve(MARKDOWN)
    }))
    vi.stubGlobal('fetch', fetchMock)

    const { result } = renderHook(() => useChapter(CHAPTER))
    expect(result.current.status).toBe('loading')
    await waitFor(() => expect(result.current.status).toBe('success'))
    expect(result.current.html).toContain('<h1>第一章 草稿层</h1>')
    expect(result.current.error).toBeNull()
  })

  it('同一章第二次挂载不再 fetch（命中缓存）', async () => {
    const fetchMock = vi.fn(() => Promise.resolve({
      ok: true, text: () => Promise.resolve(MARKDOWN)
    }))
    vi.stubGlobal('fetch', fetchMock)

    const r1 = renderHook(() => useChapter(CHAPTER))
    await waitFor(() => expect(r1.result.current.status).toBe('success'))
    r1.unmount()

    const r2 = renderHook(() => useChapter(CHAPTER))
    await waitFor(() => expect(r2.result.current.status).toBe('success'))

    expect(fetchMock).toHaveBeenCalledTimes(1)
  })

  it('fetch 404 时 status=error', async () => {
    vi.stubGlobal('fetch', () => Promise.resolve({ ok: false, status: 404 }))
    const { result } = renderHook(() => useChapter(CHAPTER))
    await waitFor(() => expect(result.current.status).toBe('error'))
    expect(result.current.html).toBe('')
  })
})
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd reader && npx vitest run test/hooks/useChapter.test.js`
Expected: FAIL，`Cannot find module '../../src/hooks/useChapter.js'`。

- [ ] **Step 3: 实现 `reader/src/hooks/useChapter.js`**

模块级 Map 缓存：key=章号，value=HTML 字符串。命中直接 success，不打 fetch。

```js
import { useEffect, useState } from 'react'
import { buildChapterUrl } from '../lib/raw.js'
import { renderMarkdown } from '../lib/markdown.js'

// 模块级缓存：章号 -> HTML。同章多次挂载只 fetch 一次。
const cache = new Map()

// 测试用：清空缓存
export function __resetChapterCache() {
  cache.clear()
}

// 加载单章。chapter: { num, title } | null
export function useChapter(chapter) {
  const num = chapter?.num
  const [html, setHtml] = useState(() => (num && cache.has(num)) ? cache.get(num) : '')
  const [status, setStatus] = useState(() => (num && cache.has(num)) ? 'success' : 'loading')
  const [error, setError] = useState(null)

  useEffect(() => {
    if (!num) return
    if (cache.has(num)) {
      setHtml(cache.get(num))
      setStatus('success')
      return
    }
    let cancelled = false
    setStatus('loading')
    setError(null)
    fetch(buildChapterUrl(chapter))
      .then(res => {
        if (!res.ok) throw new Error(`章节加载失败 (HTTP ${res.status})`)
        return res.text()
      })
      .then(md => {
        const out = renderMarkdown(md)
        cache.set(num, out)
        if (!cancelled) { setHtml(out); setStatus('success') }
      })
      .catch(e => {
        if (!cancelled) { setError(e.message || '未知错误'); setStatus('error') }
      })
    return () => { cancelled = true }
  }, [num, chapter])

  return { html, status, error }
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd reader && npx vitest run test/hooks/useChapter.test.js`
Expected: PASS，3 个用例全绿。

> 注意：`useEffect` 依赖数组里 `chapter` 是对象每次新建，但 `num` 是字符串稳定值，缓存命中分支靠 `cache.has(num)` 提前返回，不会重复 fetch。如测试发现重复 fetch，把依赖数组改为 `[num]` 并在 effect 内用 `buildChapterUrl(chapter)` 改为从缓存/索引重建，但当前实现以测试通过为准。

- [ ] **Step 5: 提交**

```bash
cd /d/workspace/ai/webnovel
git add reader/src/hooks/useChapter.js reader/test/hooks/useChapter.test.js
git commit -m "feat(reader): useChapter hook + 缓存 + 测试"
```

---

## Task 7: useReadingProgress hook（TDD）

**Files:**
- Create: `reader/src/hooks/useReadingProgress.js`
- Create: `reader/test/hooks/useReadingProgress.test.js`

读写 localStorage 的当前章号 + 滚动位置。对外暴露 getter/setter，不碰 DOM。

- [ ] **Step 1: 写失败测试 `reader/test/hooks/useReadingProgress.test.js`**

```js
import { describe, it, expect, beforeEach } from 'vitest'
import { renderHook, act } from '@testing-library/react'
import { useReadingProgress } from '../../src/hooks/useReadingProgress.js'
import { PROGRESS_KEY } from '../../src/hooks/useReadingProgress.js'

describe('useReadingProgress', () => {
  beforeEach(() => { localStorage.clear() })

  it('无记录时返回 null 章号', () => {
    const { result } = renderHook(() => useReadingProgress())
    expect(result.current.num).toBeNull()
  })

  it('save 后 get 能读回章号与滚动位置', () => {
    const { result } = renderHook(() => useReadingProgress())
    act(() => result.current.save('021', 1234))
    expect(result.current.num).toBe('021')
    expect(result.current.scrollTop).toBe(1234)
  })

  it('持久化到 localStorage 的 key 为 PROGRESS_KEY', () => {
    const { result } = renderHook(() => useReadingProgress())
    act(() => result.current.save('005', 100))
    expect(JSON.parse(localStorage.getItem(PROGRESS_KEY))).toEqual({ num: '005', scrollTop: 100 })
  })
})
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd reader && npx vitest run test/hooks/useReadingProgress.test.js`
Expected: FAIL，`Cannot find module`。

- [ ] **Step 3: 实现 `reader/src/hooks/useReadingProgress.js`**

```js
import { useState, useCallback } from 'react'

export const PROGRESS_KEY = 'sxcb-progress'

function read() {
  try {
    const raw = localStorage.getItem(PROGRESS_KEY)
    if (!raw) return { num: null, scrollTop: 0 }
    const o = JSON.parse(raw)
    return { num: o.num ?? null, scrollTop: o.scrollTop ?? 0 }
  } catch {
    return { num: null, scrollTop: 0 }
  }
}

// 读写阅读位置（章号 + 滚动位置），持久化到 localStorage
export function useReadingProgress() {
  const [state, setState] = useState(read)

  const save = useCallback((num, scrollTop) => {
    const next = { num, scrollTop }
    localStorage.setItem(PROGRESS_KEY, JSON.stringify(next))
    setState(next)
  }, [])

  return { ...state, save }
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd reader && npx vitest run test/hooks/useReadingProgress.test.js`
Expected: PASS，3 个用例全绿。

- [ ] **Step 5: 提交**

```bash
cd /d/workspace/ai/webnovel
git add reader/src/hooks/useReadingProgress.js reader/test/hooks/useReadingProgress.test.js
git commit -m "feat(reader): useReadingProgress hook + 测试"
```

---

## Task 8: 组件实现（ChapterList / ChapterView / NavButtons / Toolbar / LoadingError）

**Files:**
- Create: `reader/src/components/LoadingError.jsx`
- Create: `reader/src/components/ChapterList.jsx`
- Create: `reader/src/components/ChapterView.jsx`
- Create: `reader/src/components/NavButtons.jsx`
- Create: `reader/src/components/Toolbar.jsx`

组件层纯展示 + 组合 hooks，不含数据获取逻辑。为保证组件契约，给 `ChapterList` 与 `NavButtons` 写轻量渲染测试（断言交互回调被调用），其余组件靠人工/集成验证。

- [ ] **Step 1: 写 ChapterList 与 NavButtons 的渲染测试 `reader/test/components/ChapterList.test.jsx`**

```jsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
import { ChapterList } from '../../src/components/ChapterList.jsx'
import { NavButtons } from '../../src/components/NavButtons.jsx'

describe('ChapterList', () => {
  const CHAPTERS = [
    { num: '001', title: '草稿层' },
    { num: '002', title: '十秒' }
  ]
  it('渲染所有章节标题', () => {
    render(<ChapterList chapters={CHAPTERS} currentNum="001" onSelect={() => {}} />)
    expect(screen.getByText('第001章 草稿层')).toBeTruthy()
    expect(screen.getByText('第002章 十秒')).toBeTruthy()
  })
  it('点击章节触发 onSelect(num)', () => {
    const onSelect = vi.fn()
    render(<ChapterList chapters={CHAPTERS} currentNum="001" onSelect={onSelect} />)
    fireEvent.click(screen.getByText('第002章 十秒'))
    expect(onSelect).toHaveBeenCalledWith('002')
  })
  it('当前章有高亮 class', () => {
    render(<ChapterList chapters={CHAPTERS} currentNum="002" onSelect={() => {}} />)
    const item = screen.getByText('第002章 十秒').closest('li')
    expect(item.className).toContain('active')
  })
})

describe('NavButtons', () => {
  it('上一章/下一章按钮存在且触发回调', () => {
    const onPrev = vi.fn()
    const onNext = vi.fn()
    render(<NavButtons hasPrev={true} hasNext={true} onPrev={onPrev} onNext={onNext} />)
    fireEvent.click(screen.getByText('上一章'))
    expect(onPrev).toHaveBeenCalled()
    fireEvent.click(screen.getByText('下一章'))
    expect(onNext).toHaveBeenCalled()
  })
  it('hasPrev=false 时上一章按钮 disabled', () => {
    render(<NavButtons hasPrev={false} hasNext={true} onPrev={() => {}} onNext={() => {}} />)
    expect(screen.getByText('上一章').disabled).toBe(true)
  })
})
```

> `.jsx` 测试文件需要 Vite 的 React 插件支持，已在 `vite.config.js` 注册 `@vitejs/plugin-react`，Vitest 自动接管 JSX。

- [ ] **Step 2: 运行测试验证失败**

Run: `cd reader && npx vitest run test/components/ChapterList.test.jsx`
Expected: FAIL，模块找不到。

- [ ] **Step 3: 实现 `reader/src/components/LoadingError.jsx`**

```jsx
export function LoadingError({ status, error, onRetry }) {
  if (status === 'loading') {
    return <div className="state loading">加载中…</div>
  }
  if (status === 'error') {
    return (
      <div className="state error">
        <p>{error || '加载失败'}</p>
        <button onClick={onRetry}>重试</button>
      </div>
    )
  }
  return null
}
```

- [ ] **Step 4: 实现 `reader/src/components/ChapterList.jsx`**

```jsx
export function ChapterList({ chapters, currentNum, onSelect }) {
  return (
    <nav className="chapter-list">
      <ul>
        {chapters.map(ch => (
          <li key={ch.num} className={ch.num === currentNum ? 'active' : ''}>
            <button onClick={() => onSelect(ch.num)}>
              第{ch.num}章 {ch.title}
            </button>
          </li>
        ))}
      </ul>
    </nav>
  )
}
```

- [ ] **Step 5: 实现 `reader/src/components/ChapterView.jsx`**

```jsx
import { LoadingError } from './LoadingError.jsx'

export function ChapterView({ chapter, status, error, html, onRetry }) {
  return (
    <article className="chapter-view">
      <LoadingError status={status} error={error} onRetry={onRetry} />
      {status === 'success' && chapter && (
        <>
          <h1 className="chapter-title">第{chapter.num}章 {chapter.title}</h1>
          <div className="chapter-body" dangerouslySetInnerHTML={{ __html: html }} />
        </>
      )}
    </article>
  )
}
```

- [ ] **Step 6: 实现 `reader/src/components/NavButtons.jsx`**

```jsx
export function NavButtons({ hasPrev, hasNext, onPrev, onNext }) {
  return (
    <div className="nav-buttons">
      <button onClick={onPrev} disabled={!hasPrev}>上一章</button>
      <button onClick={onNext} disabled={!hasNext}>下一章</button>
    </div>
  )
}
```

- [ ] **Step 7: 实现 `reader/src/components/Toolbar.jsx`**

字号三档（small/medium/large），主题切换按钮，回顶按钮。

```jsx
const FONT_SIZES = [
  { key: 'small', label: '小' },
  { key: 'medium', label: '中' },
  { key: 'large', label: '大' }
]

export function Toolbar({ theme, onToggleTheme, fontSize, onChangeFontSize, onBackToTop }) {
  return (
    <div className="toolbar">
      <div className="font-sizes">
        {FONT_SIZES.map(fs => (
          <button
            key={fs.key}
            className={fontSize === fs.key ? 'active' : ''}
            onClick={() => onChangeFontSize(fs.key)}
          >{fs.label}</button>
        ))}
      </div>
      <button onClick={onToggleTheme}>
        {theme === 'dark' ? '☀ 浅色' : '☾ 深色'}
      </button>
      <button onClick={onBackToTop}>↑ 顶部</button>
    </div>
  )
}
```

- [ ] **Step 8: 运行组件测试验证通过**

Run: `cd reader && npx vitest run test/components/ChapterList.test.jsx`
Expected: PASS，ChapterList 3 个 + NavButtons 2 个用例全绿。

- [ ] **Step 9: 提交**

```bash
cd /d/workspace/ai/webnovel
git add reader/src/components/ reader/test/components/
git commit -m "feat(reader): 展示组件 + 渲染测试"
```

---

## Task 9: App 组装 + 样式 + 键盘翻章

**Files:**
- Modify: `reader/src/App.jsx`
- Modify: `reader/src/App.css`

App 把所有 hooks 与组件组合：解析 URL `?ch=` 决定初始章；切章时更新 URL 与进度；键盘 ←/→ 翻章；字号/主题持久化；滚动位置记忆。

- [ ] **Step 1: 重写 `reader/src/App.jsx`**

```jsx
import { useState, useEffect, useCallback, useRef } from 'react'
import { useChapters } from './hooks/useChapters.js'
import { useChapter } from './hooks/useChapter.js'
import { useReadingProgress } from './hooks/useReadingProgress.js'
import { getTheme, setTheme } from './lib/theme.js'
import { ChapterList } from './components/ChapterList.jsx'
import { ChapterView } from './components/ChapterView.jsx'
import { NavButtons } from './components/NavButtons.jsx'
import { Toolbar } from './components/Toolbar.jsx'
import './App.css'

const FONT_SIZE_KEY = 'sxcb-fontsize'

function readUrlNum() {
  const p = new URLSearchParams(window.location.search)
  return p.get('ch')
}
function writeUrlNum(num) {
  const url = new URL(window.location.href)
  url.searchParams.set('ch', num)
  window.history.replaceState(null, '', url)
}

export default function App() {
  const { chapters, status: listStatus, error: listError, retry: retryList } = useChapters()
  const progress = useReadingProgress()

  // 初始章号：URL > 进度 > 第一章
  const [currentNum, setCurrentNum] = useState(() => {
    return readUrlNum() || progress.num || null
  })

  // 章号选定后，找到对应 chapter 对象
  const currentChapter = chapters.find(c => c.num === currentNum) || null
  const { html, status: chStatus, error: chError } = useChapter(currentChapter)

  // 主题
  const [theme, setThemeState] = useState(() => getTheme())
  useEffect(() => {
    document.documentElement.dataset.theme = theme
  }, [theme])
  const toggleTheme = useCallback(() => {
    const next = theme === 'dark' ? 'light' : 'dark'
    setTheme(next)
    setThemeState(next)
  }, [theme])

  // 字号
  const [fontSize, setFontSize] = useState(() => localStorage.getItem(FONT_SIZE_KEY) || 'medium')
  useEffect(() => {
    localStorage.setItem(FONT_SIZE_KEY, fontSize)
    document.documentElement.dataset.fontSize = fontSize
  }, [fontSize])

  // 索引就绪后若还没选章，选第一章或进度章
  useEffect(() => {
    if (listStatus === 'success' && !currentNum && chapters.length) {
      setCurrentNum(progress.num || chapters[0].num)
    }
  }, [listStatus, currentNum, chapters, progress.num])

  // 切章：更新状态 + URL + 进度
  const selectChapter = useCallback((num) => {
    setCurrentNum(num)
    writeUrlNum(num)
    progress.save(num, 0)
    window.scrollTo(0, 0)
  }, [progress])

  // 邻章
  const idx = chapters.findIndex(c => c.num === currentNum)
  const hasPrev = idx > 0
  const hasNext = idx >= 0 && idx < chapters.length - 1
  const goPrev = useCallback(() => { if (hasPrev) selectChapter(chapters[idx - 1].num) }, [hasPrev, chapters, idx, selectChapter])
  const goNext = useCallback(() => { if (hasNext) selectChapter(chapters[idx + 1].num) }, [hasNext, chapters, idx, selectChapter])

  // 键盘 ←/→
  useEffect(() => {
    const onKey = (e) => {
      if (e.key === 'ArrowLeft') goPrev()
      if (e.key === 'ArrowRight') goNext()
    }
    window.addEventListener('keydown', onKey)
    return () => window.removeEventListener('keydown', onKey)
  }, [goPrev, goNext])

  // 滚动位置记忆：切换章/滚动时存
  const scrollTimer = useRef(null)
  useEffect(() => {
    if (chStatus !== 'success') return
    const onScroll = () => {
      if (scrollTimer.current) clearTimeout(scrollTimer.current)
      scrollTimer.current = setTimeout(() => {
        progress.save(currentNum, window.scrollY)
      }, 300)
    }
    window.addEventListener('scroll', onScroll)
    return () => window.removeEventListener('scroll', onScroll)
  }, [chStatus, currentNum, progress])

  // 进入时若有进度滚动位置，恢复
  const restored = useRef(false)
  useEffect(() => {
    if (chStatus === 'success' && !restored.current && progress.num === currentNum && progress.scrollTop) {
      window.scrollTo(0, progress.scrollTop)
      restored.current = true
    }
  }, [chStatus, currentNum, progress])

  const backToTop = useCallback(() => window.scrollTo({ top: 0, behavior: 'smooth' }), [])

  return (
    <div className={`app layout-${fontSize}`}>
      <Toolbar
        theme={theme}
        onToggleTheme={toggleTheme}
        fontSize={fontSize}
        onChangeFontSize={setFontSize}
        onBackToTop={backToTop}
      />
      <div className="main">
        <aside className="sidebar">
          <h2 className="book-title">时序残本</h2>
          <ChapterList chapters={chapters} currentNum={currentNum} onSelect={selectChapter} />
        </aside>
        <main className="content">
          {listStatus === 'error'
            ? <ChapterView status="error" error={listError} onRetry={retryList} />
            : currentChapter
              ? <ChapterView
                  chapter={currentChapter}
                  status={chStatus}
                  error={chError}
                  html={html}
                  onRetry={() => {/* useChapter 内部缓存，重试需切章再切回；简单实现留空 */}}
                />
              : <div className="state loading">加载中…</div>}
          {currentChapter && (
            <NavButtons hasPrev={hasPrev} hasNext={hasNext} onPrev={goPrev} onNext={goNext} />
          )}
        </main>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: 重写 `reader/src/App.css`**

完整样式：明暗主题、字号三档、侧栏 + 正文双栏、移动端单栏。

```css
:root {
  --bg: #fafaf7;
  --fg: #1a1a1a;
  --muted: #666;
  --border: #e0ddd5;
  --accent: #8b5a2b;
}
:root[data-theme="dark"] {
  --bg: #1a1a1a;
  --fg: #e8e6e0;
  --muted: #999;
  --border: #333;
  --accent: #c8965a;
}
:root[data-font-size="small"] { --reader-size: 16px; }
:root[data-font-size="medium"] { --reader-size: 18px; }
:root[data-font-size="large"] { --reader-size: 20px; }

* { box-sizing: border-box; }
html { scroll-behavior: smooth; }
body {
  margin: 0;
  background: var(--bg);
  color: var(--fg);
  font-family: system-ui, -apple-system, "PingFang SC", "Microsoft YaHei", serif;
}

.app { min-height: 100vh; font-size: var(--reader-size); }

.toolbar {
  position: sticky; top: 0; z-index: 10;
  display: flex; gap: 0.5rem; align-items: center;
  padding: 0.5rem 1rem;
  background: var(--bg); border-bottom: 1px solid var(--border);
}
.toolbar button {
  font-size: 0.85rem; padding: 0.3rem 0.7rem;
  background: transparent; color: var(--fg); border: 1px solid var(--border);
  border-radius: 4px; cursor: pointer;
}
.toolbar button.active { border-color: var(--accent); color: var(--accent); }
.toolbar .font-sizes { display: flex; gap: 0.25rem; margin-right: auto; }

.main { display: flex; max-width: 1080px; margin: 0 auto; }
.sidebar {
  width: 240px; flex-shrink: 0; padding: 1rem;
  border-right: 1px solid var(--border); height: calc(100vh - 48px);
  overflow-y: auto; position: sticky; top: 48px;
}
.book-title { font-size: 1.1rem; margin: 0 0 1rem; color: var(--accent); }
.chapter-list ul { list-style: none; padding: 0; margin: 0; }
.chapter-list li { margin: 0.1rem 0; }
.chapter-list button {
  width: 100%; text-align: left; padding: 0.4rem 0.5rem;
  background: transparent; color: var(--fg); border: none; border-radius: 4px;
  cursor: pointer; font-size: 0.9rem;
}
.chapter-list li.active button { background: var(--accent); color: #fff; }
.chapter-list li:hover button { background: var(--border); }

.content { flex: 1; padding: 1.5rem 2rem; max-width: 720px; }
.chapter-title { font-size: 1.4rem; border-bottom: 1px solid var(--border); padding-bottom: 0.5rem; }
.chapter-body { line-height: 1.9; }
.chapter-body p { margin: 0 0 1em; text-indent: 2em; }

.nav-buttons { display: flex; justify-content: space-between; margin: 2rem 0; }
.nav-buttons button {
  padding: 0.5rem 1.5rem; font-size: 0.95rem;
  background: transparent; color: var(--fg); border: 1px solid var(--border);
  border-radius: 4px; cursor: pointer;
}
.nav-buttons button:disabled { opacity: 0.4; cursor: not-allowed; }

.state { padding: 3rem 1rem; text-align: center; color: var(--muted); }
.state.error button { margin-top: 0.5rem; padding: 0.4rem 1rem; cursor: pointer; }

@media (max-width: 720px) {
  .main { flex-direction: column; }
  .sidebar { width: auto; height: auto; position: static; border-right: none; border-bottom: 1px solid var(--border); }
  .content { padding: 1rem; max-width: 100%; }
}
```

- [ ] **Step 3: 本地验证 dev**

Run: `cd reader && npm run dev`
打开本地地址，确认：目录加载出 20 章（需先有 chapters-index.json，下一 Task 创建）；手动切换明暗、字号、上下章、键盘左右键工作。

> 此时还没有 `chapters-index.json`，目录会报"加载失败"——这是预期，Task 10 创建索引后即可。先验证样式与交互骨架不崩。

- [ ] **Step 4: 跑全量测试确认无回归**

Run: `cd reader && npm test`
Expected: 所有之前 Task 的测试仍 PASS。

- [ ] **Step 5: 提交**

```bash
cd /d/workspace/ai/webnovel
git add reader/src/App.jsx reader/src/App.css
git commit -m "feat(reader): App 组装 + 样式 + 键盘翻章 + 位置记忆"
```

---

## Task 10: 创建 chapters-index.json（内容数据）

**Files:**
- Create: `chapters-index.json`

这是内容数据通道的第一份文件，躺在 main 分支根目录，浏览器运行时拉取。手动维护，加新章时追加一行。

- [ ] **Step 1: 用脚本从现有正文文件名生成索引**

先列出当前所有正文章节，确认文件名格式。Run:
```bash
ls "/d/workspace/ai/webnovel/novels/时序残本/正文/"
```
Expected: 看到 `第001章-草稿层.md` ... `第020章-异常上升.md` 共 20 个文件，格式 `第NNN章-标题.md`。

- [ ] **Step 2: 生成 chapters-index.json**

用 Python 解析文件名（避免手敲中文出错），输出到仓库根：

```bash
python3 - <<'PY'
import os, re, json
d = r"D:\workspace\ai\webnovel\novels\时序残本\正文"
pat = re.compile(r'^第(\d+)章-(.+)\.md$')
out = []
for name in sorted(os.listdir(d)):
    m = pat.match(name)
    if m:
        out.append({"num": m.group(1), "title": m.group(2)})
with open(r"D:\workspace\ai\webnovel\chapters-index.json", "w", encoding="utf-8") as f:
    json.dump(out, f, ensure_ascii=False, indent=2)
print(f"写入 {len(out)} 章")
PY
```
Expected: 输出 `写入 20 章`，`chapters-index.json` 含 20 条。

- [ ] **Step 3: 校验索引与文件名一一对应**

```bash
python3 - <<'PY'
import json, os
idx = json.load(open(r"D:\workspace\ai\webnovel\chapters-index.json", encoding="utf-8"))
d = r"D:\workspace\ai\webnovel\novels\时序残本\正文"
missing = []
for c in idx:
    fn = f"第{c['num']}章-{c['title']}.md"
    if not os.path.exists(os.path.join(d, fn)):
        missing.append(fn)
print("缺失文件:", missing or "无，全部匹配")
PY
```
Expected: `缺失文件: 无，全部匹配`

- [ ] **Step 4: 提交索引**

```bash
cd /d/workspace/ai/webnovel
git add chapters-index.json
git commit -m "feat: 章节索引 chapters-index.json（20 章）"
```

- [ ] **Step 5: 推送 main 并验证 raw 可访问**

```bash
git push origin main
```
push 后等 raw CDN 刷新（即时或最多 5 分钟），用 curl 验证：

```bash
curl -sS "https://raw.githubusercontent.com/xSmile-art/shixu-canben/main/chapters-index.json" | head -5
```
Expected: 打印出索引 JSON 前 5 行。

---

## Task 11: GitHub Actions 部署到 gh-pages

**Files:**
- Create: `.github/workflows/deploy.yml`
- Create: `.github/workflows/ci.yml`

deploy.yml：push 到 main 且改动了 `reader/` 或 `.github/workflows/` 时，构建并部署到 gh-pages 分支。用现成的 `peaceiris/actions-gh-pages` action，省去手写 git push。

- [ ] **Step 1: 创建 `.github/workflows/deploy.yml`**

```yaml
name: Deploy Reader to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'reader/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: deploy-pages
  cancel-in-progress: true

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: reader/package-lock.json
      - name: Install
        working-directory: reader
        run: npm ci
      - name: Test
        working-directory: reader
        run: npm test
      - name: Build
        working-directory: reader
        run: npm run build
      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: reader/dist
```

- [ ] **Step 2: 创建 `.github/workflows/ci.yml`（PR/push 跑测试，不部署）**

```yaml
name: CI

on:
  push:
    branches: [main]
    paths:
      - 'reader/**'
  pull_request:
    paths:
      - 'reader/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: reader/package-lock.json
      - working-directory: reader
        run: npm ci
      - working-directory: reader
        run: npm test
```

- [ ] **Step 3: 确保 package-lock.json 已提交**

`npm ci` 依赖 lockfile。若前面 Task 未生成，运行：
```bash
cd reader && npm install --package-lock-only
```
确认 `reader/package-lock.json` 存在并已 git add。

- [ ] **Step 4: 提交 workflow**

```bash
cd /d/workspace/ai/webnovel
git add .github/workflows/ reader/package-lock.json
git commit -m "ci: 部署阅读器到 gh-pages + 测试工作流"
git push origin main
```

- [ ] **Step 5: 验证 Actions 运行成功**

Run: `export PATH="$PATH:/c/Program Files/GitHub CLI" && gh run watch $(gh run list --workflow=deploy.yml --limit 1 --json databaseId -q '.[0].databaseId')`
Expected: Run 完成，状态 `success`。

若失败：`gh run view <id> --log-failed` 查日志。

- [ ] **Step 6: 配置 GitHub Pages 指向 gh-pages 分支**

用 gh API 设置 Pages 源为 gh-pages 分支 root（无需手动点 UI）：

```bash
export PATH="$PATH:/c/Program Files/GitHub CLI"
gh api -X POST repos/xSmile-art/shixu-canben/pages -f build_type=legacy -f source[branch]=gh-pages -f source[path]=/
```
> 若提示 already exists，改用：
> `gh api -X PUT repos/xSmile-art/shixu-canben/pages -f build_type=legacy -f source[branch]=gh-pages -f source[path]=/`

Expected: 返回 JSON 含 `"source": {"branch": "gh-pages", "path": "/"}`。

- [ ] **Step 7: 验证页面可访问**

等 1-2 分钟 Pages 生效后：
```bash
curl -sS -o /dev/null -w "HTTP %{http_code}\n" "https://xsmile-art.github.io/shixu-canben/"
```
Expected: `HTTP 200`。

打开浏览器访问 `https://xsmile-art.github.io/shixu-canben/`，应能看到目录 20 章 + 点开能读正文。

---

## Task 12: 验收与文档收尾

**Files:**
- Modify: `README.md`
- Create: `reader/README.md`

- [ ] **Step 1: 端到端验收清单**

逐项人工验证（在 https://xsmile-art.github.io/shixu-canben/ 上）：

- [ ] 首屏加载出左侧 20 章目录
- [ ] 默认显示第 1 章
- [ ] 点击目录某章，正文切换
- [ ] 上一章/下一章按钮工作，首章时上一章禁用、末章时下一章禁用
- [ ] 键盘 ← → 翻章
- [ ] 明暗主题切换，刷新后保持
- [ ] 字号小/中/大切换，刷新后保持
- [ ] 顶部按钮滚到顶
- [ ] 滚动一段后刷新页面，恢复到上次章 + 位置
- [ ] URL 含 `?ch=NNN`，刷新仍在该章
- [ ] 第 20 章正文完整渲染（中文段落、标题正确）

- [ ] **Step 2: 模拟"加新章"流程验证核心承诺**

本地新建 `novels/时序残本/正文/第021章-测试.md`（随便写几行），在 `chapters-index.json` 加 `{"num":"021","title":"测试"}`，push：

```bash
cd /d/workspace/ai/webnovel
# 编辑两个文件后
git add novels/时序残本/正文/第021章-测试.md chapters-index.json
git commit -m "test: 模拟加第21章验证不重新构建"
git push origin main
```
Expected：**不触发 deploy.yml**（因为没动 `reader/`），但网页在 raw CDN 刷新后（最多 5 分钟）能看到第 21 章。这验证了"加章不重新构建站点"的核心承诺。

验证能看到第 21 章后，把这个测试章撤掉：
```bash
git rm "novels/时序残本/正文/第021章-测试.md"
# 编辑 chapters-index.json 删掉第021行
git add -A
git commit -m "chore: 移除测试章"
git push origin main
```

- [ ] **Step 3: 更新仓库 README.md**

在现有 README 末尾追加"在线阅读"小节：

```markdown
## 在线阅读

部署在 GitHub Pages：https://xsmile-art.github.io/shixu-canben/

### 加新章节

1. 写 `novels/时序残本/正文/第NNN章-标题.md`
2. 在 `chapters-index.json` 追加 `{"num":"NNN","title":"标题"}`
3. `git push origin main`

网页在 raw CDN 刷新后（最多 5 分钟）即可读到新章，无需重新构建站点。React 阅读器代码在 `reader/`，改代码才会触发部署。
```

- [ ] **Step 4: 创建 reader/README.md**

```markdown
# 时序残本 · 在线阅读器

基于 Vite + React 的静态阅读器，部署在 GitHub Pages。

## 本地开发

\`\`\`bash
cd reader
npm install
npm run dev
\`\`\`

## 测试

\`\`\`bash
npm test
\`\`\`

## 构建

\`\`\`bash
npm run build
\`\`\`

## 架构要点

站点本体与内容数据解耦：章节正文和索引以原始 markdown/json 形式躺在仓库 `main` 分支，浏览器运行时通过 `raw.githubusercontent.com` 拉取。加新章只需 push 正文与索引，不重新构建站点。

详见 `docs/superpowers/specs/2026-07-21-reader-design.md`。
```

- [ ] **Step 5: 提交文档**

```bash
cd /d/workspace/ai/webnovel
git add README.md reader/README.md
git commit -m "docs: 在线阅读器说明 + 加章流程"
git push origin main
```

- [ ] **Step 6: 完成**

全部 Task 完成，阅读器上线。

---

## Self-Review

**1. Spec coverage:**
- 章节目录 + 进度 → Task 8 ChapterList + Task 7 useReadingProgress ✓
- 上下章切换 → Task 8 NavButtons + Task 9 App 键盘/按钮 ✓
- 字号调节 + 顶部跳转 → Task 8 Toolbar + Task 9 ✓
- 本地记忆阅读位置 → Task 7 + Task 9 滚动记忆 ✓
- 手动明暗主题 → Task 4 + Task 9 ✓
- 仅展示正文（不含设定） → 只读 `正文/*.md`，Task 2 raw.js 路径限定 ✓
- 运行时拉取 raw，不重新构建 → Task 2/5/6 + Task 11 deploy 只在改 reader 时触发 ✓
- 仓库公开 → 已在实现前用 gh 设为 Public ✓
- 加章流程 → Task 10 + Task 12 Step 2 验证 ✓
- GitHub Pages 部署 → Task 11 ✓
- 无设定集/搜索/用户系统 → 全程未实现，符合 YAGNI ✓

**2. Placeholder scan:** 无 TBD/TODO/泛化措辞。Task 9 中 `onRetry` 留空有注释说明（useChapter 缓存导致重试需切章再切回，简单实现留空）——这是有意的范围裁剪，非占位符。可接受。

**3. Type consistency:**
- `useChapters` 返回 `{ chapters, status, error, retry }`——Task 9 App 用到 `status/error/retry/chapters` 一致 ✓
- `useChapter(chapter)` 返回 `{ html, status, error }`——Task 9 用 `html/status/error` 一致 ✓
- `useReadingProgress` 返回 `{ num, scrollTop, save }`——Task 9 用 `progress.num/save/scrollTop` 一致 ✓
- `buildFileName/buildChapterUrl/buildIndexUrl` 签名 Task 2 定义，Task 5/6 调用一致 ✓
- `THEME_KEY/PROGRESS_KEY` 导出名在定义与测试间一致 ✓

无遗留问题，计划可交付执行。
